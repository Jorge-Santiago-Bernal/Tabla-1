#Este programa dibuja un cuadrado de las dimensiones que le digamos
def cuadrados():
  n_filas=int(input("¿De qué tamaño quieres el cuadrado? (1-10)"))
  #Esto es un bucle anidado (nested)
  for cont in range(1,n_filas+1):
    for col in range(1,n_filas+1):
      print('* ',end='') 
    print("") # \n hace un salto de línea


cuadrados()
